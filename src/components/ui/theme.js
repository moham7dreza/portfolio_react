import {createTheme} from "@mui/material";

export const theme = createTheme({
    direction: 'rtl',
    palette: {
        mode: 'dark',
        primary: {
            main: '#8be9fd'
        },
        secondary: {
            main: '#50fa7b'
        },
    },
    typography: {
        fontFamily: 'Vazirmatn'
    }
});